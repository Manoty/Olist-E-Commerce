version: 2

models:
  - name: stg_orders__orders
    description: Cleaned orders with fulfillment metrics
    columns:
      - name: order_id
        description: Unique order identifier
        tests:
          - unique
          - not_null
      - name: customer_id
        description: Foreign key to customer
        tests:
          - not_null